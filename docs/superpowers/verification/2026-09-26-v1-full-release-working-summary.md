# Koi Engine 1.0.0 full-release working summary (2026-09-26)

## Source and decisions

- Default branch `koi-engine` at start: `84dd8d57f62ba96315e6634be45148f5bd39fc29`; public pre-release now uses bare tag `1.0.0` at that commit. User approved moving the tag to the final commit only after all full-release gates. The release currently has only Windows/Linux archives and their `.sha256` files; evidence must remain local, never in its body/assets.
- User requires: zero of 12 known search XFAILs and 3 intermittent exemptions; review invalid timing/SMP assertions but preserve behavior coverage; hard-gate classical CPU, CPU NNUE v4/v5, and actual GPU NNUE v5; use fixtures for unavailable real book/Syzygy assets; ≥1,320 fresh games; one-sided 95% strength lower bound > −10 Elo; mandatory local Docker package smoke; `OwnBook=false` default.
- Commit only on `koi-engine` with detailed messages and no new branch; revert a pushed regression. Preserve untracked `docs/superpowers/verification/2026-09-26-repository-exploration-working-summary.md`. Existing managed worktree may be fast-forwarded for clean packaging but receives no commits.
- Spec: `docs/superpowers/specs/2026-09-26-v1-full-release-reliability-design.md`; plan: `docs/superpowers/plans/2026-09-26-v1-full-release-reliability.md`.

## Architecture and existing evidence

- Search work centers on `src/koi/detail/search_runner.cpp`, `search_context.cpp`, policy/TT code, and `tests/unit/search/koi_search_tests.cpp`. The 15 exemptions fall into root forcing/fallback, king safety/quiescence, pruning, threaded MultiPV, and tactical fixtures.
- Existing pre-release at `84dd8d5` passed Windows and Linux CI, 640 games, and the prior paired −10 Elo gate. Those are baseline evidence; source changes require fresh full-release binary/hash/game evidence.
- Host has NVIDIA GTX 1060 sm_61, CUDA 12.9 `nvcc`, GPU-enabled Release build. `koi_gpu_probe.exe` passed and `gpu_nnue_tests.exe` reported 5/5 with zero skips. Local ignored v4 and v5 network artifacts exist, but the full gate will generate deterministic test fixtures instead.
- Docker Desktop initially was stopped in this shell. Root launched it hidden, confirmed `docker info` reports `linux x86_64 29.8.0`, and `docker run --rm hello-world` passed. Full Ubuntu 22.04 package smoke awaits a settled clean source.

## Active work

- Three independent subagents are editing disjoint areas without committing: OwnBook defaults/docs/tests; deterministic NNUE fixture generator; Windows no-retry CI job. Root owns search mechanisms, Docker/package/game integration, review, and all main-branch commits.
- No full-release source code has been committed yet. Do not rely on test results reported by agents without inspecting diffs and running focused/full verification.

## First implementation checkpoint

- Commit `3028f3c` recorded the approved design/plan and added an independent Windows no-retry CI job. Root verified the CI configuration test, YAML parse, and staged diff before committing; agents' disjoint working files were not staged.
- Docker Desktop launched successfully; `docker info` reports `linux x86_64 29.8.0` and `docker run --rm hello-world` passed. The exact Ubuntu 22.04 clean-source package smoke remains pending settled source and clean staging.
- Existing Release binary with `KOI_TEST_RETRIES=1` reproduced `single-PV root forcing extension` (`f6e4`, expected `f6g4`) and serial/threaded `depth-one forcing check` (`c2c3`, expected `c2c5`) as XFAILs. A Koi depth-3 MultiPV search and Stockfish 19 depth-14 both preferred `f6g4` in the knight fixture, so that appears to be a real shallow-root defect. Stockfish 19 depth-14 scored `c2c3` and `c2c5` equally in the checking fixture, so its exact `c2c5` oracle may be invalid; preserve a deterministic non-blunder/coverage assertion instead of forcing that particular move.
- Root added diagnostic stats to two existing failing test messages; the search test rebuild and focused rerun remain pending. OwnBook and fixture-generator subagents are still editing disjoint files, and their results require root verification.
- Later naming instruction: GitHub release tags use the bare version `1.0.0`; the stable full-release title uses `Koi Engine v1.0.0`. The user chose to migrate the existing prefixed tag, and external release state now shows bare tag `1.0.0` at the original `84dd8d5` commit, title `First beta`, with exactly four platform archive/checksum assets. Root has made no release/tag mutation in this full-release task. The exact current body/assets and hashes are frozen locally in ignored `artifacts/baselines/v1.0.0-pre-full/` for strength comparison and rollback.

## Book, fixture, and first search pass

- Detailed main-branch commit `577aed5` changed runtime/UCI, match-harness, package, README, and release-note OwnBook defaults to `false`; explicit `true` book behavior remains covered. Root independently ran four focused Debug CTest entries (4/4) and 27 relevant Python tests (27/27) before committing. The user exploration summary remains untracked.
- Detailed main-branch commit `2576870` added the test-only `koi-nnue-fixture` v4/v5 serializer CLI and its integration test. Root inspected the diff and independently reran the fixture test (1/1 Debug). Generated networks remain outside the release package.
- A fresh MSVC CPU-only Release tree at `build/full-release-search` replaced the stale `build/release` Ninja cache for focused search work. The root-forcing failure persisted after candidate-band expansion because all deeper single-move verifications were marked selective/inexact (`authoritative=false`). Root added a separate publishable status and permits comparable publishable verification only for explicit fixed-depth-one quiet-root confirmation; timed roots retain their former four-candidate limit. The knight fixture now passes with bestmove/PV coherence and actual root selective research.
- Stockfish 19 depth-14 rated `c2c3` and `c2c5` equally in the check fixture; reviewed depth-one tests now accept either reviewed move but require complete PV and candidate coverage. Both serial/threaded cases pass. The poisoned-capture quiescence case became XPASS in the full Release shard and five separate focused runs, so its stale XFAIL entry was removed; it passes as a normal test.
- The root-forcing cluster's no-retry Release search shards passed 4/4 in 153.50 s. The updated Release engine also passed `koi_engine_process` and `koi_engine_time_safety_process` 2/2. The next open king-safety case still chooses `b7e4`; Stockfish 19 depth-16 prefers legal escape `c1b2`, so this is a plausible real search defect rather than an exact-move tie.

## Mode evidence and timing checkpoint

- Detailed main-branch commit `d447505` added `-KoiEvaluatorMode classical|nnue-v4|nnue-v5|gpu-v5` and `-KoiEvalFile` attestation to UCI match artifacts, with network SHA-256 and engine NNUE/GPU confirmation checks; the validator has optional expected-mode/hash arguments and preserves legacy input. Root independently ran the Python validator suite 23/23 and, after building the Debug fixture generator in its tree, the real UCI match process test 1/1. No evidence is posted in GitHub releases.
- The original two timed root cases selected different reviewed moves under 100 ms in Release versus Debug. Their test oracles now require legal, coherent completed search or explicit safe fallback; the fixed-depth tactical checks continue to cover the exact/near-tie behavior. Both revised tests passed 10/10 fresh processes in Release and Debug with retries disabled. `timed poisoned capture` passed 20/20 fresh Release and 20/20 fresh Debug processes after the root fix and was removed from the intermittent list. The intermittent registry is now empty.
- Four of the original 12 known search failures have become normal passing cases (`single-PV root forcing extension`, serial/threaded depth-one forcing checks, and poisoned capture quiescence); eight known failures remain. Search changes are still uncommitted and under read-only review.
- The published pre-release was already migrated externally to bare tag `1.0.0` at `84dd8d5`, title `First beta`, without evidence assets. Root downloaded its four assets and release metadata to ignored `artifacts/baselines/v1.0.0-pre-full/`; archive and launcher hashes and clean provenance were independently verified. The local `2026-09-26-v1.0.0-beta-baseline.md` records them for strength comparison and rollback. No release mutation was made by root.

## Root-score review and next search work

- A read-only review found the proposed `RootVerification.publishable` flag was only PV coverage and could not justify comparing scores or claiming a lower bound. Diagnostic one-move depth-3 searches returned `f6e4=-59` and `f6g4=-28`, both selective with no exact/upper certificate. The root depth-one pass itself returned `f6e4=-26` and `f6g4=-28`; only `f6g4` received the quiet forcing extension. An attempted extra root extension did not change the selected move and was reverted.
- Reworked fixed-depth-one tactical confirmation to treat complete selective lines as move-selection estimates, not score proofs. The selected nominal-depth PV is refreshed; any estimate used clears root authority, and non-authoritative UCI info no longer asserts `lowerbound`. The new `selective root score provenance` UCI regression failed before the bound change and passed afterward; the original knight regression failed under the strict-only gate and passed with the bounded tactical estimate. Both are focused evidence only; broad Release/Debug/process reruns are still required after the latest source change.
- The `sparse phase-rich null safety` test's exact move/centipawn pin was stale (`b2e2/133` in Release versus `b2h8/120` expected); Debug stopped at depth 2 under its node cap. Its replacement checks completed legal coherent PV and zero null cutoffs, passed focused Debug and Release, and removed that XFAIL. Seven original known XFAILs remain: root king safety, two threaded MultiPV, king-zone LMR, opening central break, late-move full-depth verification, and committed PGN tactical fixtures.
- Linux CI has an uncommitted two-line package assertion for exact `GITHUB_SHA` and clean provenance. A parallel match-evidence change is adding package archive/source/executable binding and negative validator tests. Neither has been committed. A per-plan ignored progress ledger now lives at `.superpowers/sdd/2026-09-26-v1-full-release-reliability/progress.md`.

## Package evidence, search, and mode checkpoint

- Commit `32cc70a` added exact `GITHUB_SHA` and `source_dirty=false` assertions to the Linux container package smoke. The embedded Python initially lacked `import os`; a local extracted-body execution probe reproduced the NameError and passed after the import. YAML parse and CI configuration test passed.
- Commit `c024ffa` added opt-in package-manifest/archive binding to the UCI match report, including actual executable SHA-256, archive and manifest SHA-256, clean source commit, and negative checks for forged hashes/dirty or malformed provenance. Root independently ran Python validator tests 27/27 and Debug `koi_uci_match_process` 1/1 (104.08 s). Commit `f797243` aligned the plan/spec with the user's bare `1.0.0` tag and stable title.
- Root king safety: the depth-three path triggered and shortlisted `c1b2`, but the incumbent's depth-five verification was selective and blocked all escape checks. Stockfish 19 depth 14 found mate in 9 after current `b7e4`, while `c1b2` avoided a forced mate; Koi's incumbent and escape verifications were selective estimates. The bounded fixed-depth diagnostic path now considers complete relief lines, rejects negative mate outcomes, nominally refreshes the PV, and clears score authority. The test requires `c1b2`, king-zone relief, coherent legal PV, and `Bound::estimate`; focused Release and Debug passed. This source/test work remains uncommitted under review.
- The sparse null-safety test now checks completion/legal PV and zero null cutoffs rather than stale move/centipawn pins. The king-zone LMR test now uses an eligible quiet rook move (`h1h7`) after a capture, reaches depth four, and observes the exclusion counter. The two threaded MultiPV tests now require distinct legal ranked lines, coherent completion, and warmed-hash repeated equality without a schedule-sensitive `h1h2` rank-one pin. Root independently ran six focused Release cases with no retry; all passed. A fresh Release search-shard run passed 4/4 in 154.19 s, and rebuilt UCI/process/time-safety gates passed 3/3. Three known XFAILs remain: opening central break, late-move full-depth verification, and committed PGN tactical fixtures. No intermittent exemptions remain.
- GPU-enabled Release build configured with MSVC/CMake 3.31.6 and CUDA PTX. Actual GTX 1060 `sm_61` GPU probe passed; `gpu_nnue_tests` passed 5/5 with zero skips. Windows Debug/Release v4 fixtures matched SHA-256 `f1fd40a19f3fafccb7715671d61e655294e75fe6322b2d3eaf4b5f73c0a14aa2`, and the current v5 fixtures matched `e84a4be1d631200fb3e22a28f7b89982870d13892c26ab91001a5c33d2fe0701`. CPU v4/v5 UCI match smokes passed. GPU-v5 match smoke rejected fallback because the current v5 fixture has unsupported hidden/L1 dimensions; a separate agent is changing only the test generator to emit the GPU-supported dimensions, then the mode smoke must be rerun. These exploratory smokes do not replace final clean-package gates.
- The 320-opening sample was frozen before games at ignored `artifacts/matches/full-release/openings-seed-20260927.txt`, SHA-256 `f7dddee8ac421e2155e768f95067072397ca4a1dc345a83b3876b9703b1bf3ed`; first-10/20/40 subsets were also frozen for optional evaluator cells. No full-release gate games have started.
- The public beta now has title `Koi Engine v1.0.0 beta`, bare tag `1.0.0` still at `84dd8d5`, `prerelease=true`, `draft=false`, and the same four assets. Current API metadata was saved to ignored `artifacts/baselines/v1.0.0-pre-full/github-release-after-title.json`; no completion evidence was added to the release.

## All search exceptions cleared; final source gates pending

- The late-move fixture now invokes the real `SearchContext::negamax` depth-three cut-node path with TT disabled. The quiet non-pawn `e8f8` is the fifth reply, receives a reduced fail-high, and is rejected after full-depth verification; a separate root PV keeps `a2a3` at score zero. Focused Debug/Release passed. The king-zone LMR fixture initially omitted `search_moves_specified=true`; root found and added it, then independently verified the actual filtered Release case passes.
- The old opening test pinned e2e4/e3 as best at depth five, although root pawns are already excluded from LMR and Stockfish 19 depth 16 treats c1e3 as a plausible but weaker alternative. Replacement coverage proves the policy gate excludes late root pawn breaks and both e2e4/e3 remain searchable at full depth with legal PV; focused Release passed. The four PGN FENs were replayed to their named source games locally. Their historical exact moves/scores were not PGN truths; Stockfish 19 depth-14 reviewed move sets now replace them, and the first position searches depth five to reject its shallow e5f4 blunder. Focused Release passed.
- The `known_failures` and `intermittent` registries are now both empty. Full no-retry Release `koi_search_tests --quiet` reported `run=152 pass=152 fail=0 xfail=0 xpass=0 skip=0 unseen=0 intermittent=0`; full no-retry Debug reported the same. Root added a RED→GREEN policy regression to ensure only authoritative negative mate scores can exclude a king escape; `search_policy_tests` passed 8/8. A fresh reviewer found this issue and it was fixed. A final reviewer then found estimated info still emitted as exact-looking UCI `score`/WDL; the strengthened UCI regression failed before the fix and passed afterward. Estimated UCI info now publishes depth/nodes/PV without score/WDL, while exact score/WDL remains covered through a single legal mate move. Release UCI/controller/process/En Croissant/time-safety tests passed 4/4; Debug counterparts plus NNUE match process passed 5/5. Full search source/test/README/UCI changes are still uncommitted at this checkpoint.
- The pushed `ec14353` remote commit passed all five Linux CI jobs (GCC, Clang, modules-off, no-retry flake, tarball). Windows CI passed Release, sanitizer, shadow-diff, and no-retry flake but Debug smoke failed at NNUE v5 `readyok`: the 113 MB network exceeded the ordinary 5 s protocol wait on an oversubscribed runner. The harness now gives Koi nonclassical network readiness 30 s, or 120 s for networks at least 64 MiB, without extending move clocks/opponent deadlines. The Debug UCI match process passed locally after that change; final-commit CI must rerun. The frozen beta Linux tarball also passed a local Ubuntu 22.04 UCI smoke with one `bestmove`.
- Commit `ec14353` changed only the test-only v5 generator to hidden 1536/L1 32. New deterministic v5 fixture size is 113,302,043 bytes, Debug/Release SHA-256 `45ab9be6a873ee00eaba1864ceec6acdb46fa963c805ad667019beada5224e39`; v4 remains `f1fd40a19f3fafccb7715671d61e655294e75fe6322b2d3eaf4b5f73c0a14aa2`. GPU-enabled Release NNUE boundary/fixture/probe/parity CTest entries passed 4/4, parity executable 5/5 with zero skips. Independent CPU-v5 and actual GPU-v5 one-game mode smokes passed validator; GPU report included the active marker and no fallback.
- Ignored Windows and Linux resumable matrix runners are at `artifacts/probes/full-release-matrix.ps1` (14 legs/880 games) and `artifacts/probes/full-release-linux-matrix.ps1` (12 legs/440 games). Both default to read-only dry run and require `-RunMatches`; their dry runs checked counts and created no evidence dirs. Root reviewed and adjusted Windows runner to require a successful harness exit and explicitly set GPU env per mode. The pinned Ubuntu 22.04 PowerShell image was pulled and actually ran `/usr/bin/pwsh` 7.4.6. Final packages/source hashes do not exist yet, so no matrix games have started. A local ignored Ubuntu smoke runner extracts the exact workflow container body and will compare Linux v4/v5 fixture hashes to Windows after final commit.

## Committed search milestone and next gates

- Detailed main-branch commit `70c9482` recorded the cleared search cases, provenance-aware root choices, UCI score/WDL omission for unproven estimates, and protocol-test updates. Detailed commit `a493484` scoped the full-width NNUE readyok wait to network loading. The plan now marks Tasks 1, 2, and 4 complete; Tasks 3, 5, 6, 7, and 8 remain open. The user's original untracked exploration summary is still preserved.
- Fresh Release and Debug direct search suites each finished `run=152 pass=152 fail=0 xfail=0 xpass=0 skip=0 unseen=0 intermittent=0` with `KOI_TEST_RETRIES=1`. After the UCI estimate formatter changed, the Release controller/process/En Croissant/time-safety group passed 4/4, and the Debug group including `koi_uci_match_process` passed 5/5. These tests are local evidence; broad full Release/Debug, ASan, shadow-diff, flake repetitions, final Docker/CI, 1,320 games, and release promotion remain.
